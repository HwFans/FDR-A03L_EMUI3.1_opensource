 First, make sure the version of JDK should be 1.6.
 
 Second, compile kernel as follows: 
 Step1¡¢Extract kernel source code by using "tar -zxvf kernel.[Android.5.1].tar.gz".
 Step2¡¢Execute build.sh script to compile kernel.
 Step3¡¢Find the mirror image in out/KERNEL_OBJ/arch/arm64/boot directory after finishing compiling.